# node-nextjs-multitenant-template

QCP multitenant Next.js application template. All project conventions and hard rules:

@AGENTS.md

Full QCP standards documents live in `docs/standards/` — consult them (especially
`nextjs-multitenant-template.md`, `multi-tenancy.md` and `mifin-design-system.md`)
before generating modules, tenant logic, theming or styling changes.
