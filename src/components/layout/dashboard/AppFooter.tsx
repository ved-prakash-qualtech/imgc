/**
 * APP FOOTER
 *
 * The one line that closes every screen.
 *
 * Rendered once by `DashboardShell` rather than pasted per page. Products that copied
 * this markup into individual dashboards ended up with the footer on some screens and
 * not others; placed in the shell it cannot drift, and a page never has to remember it.
 *
 * `px-6` lines the text up with the page padding above it instead of sitting flush
 * against the window edge.
 */
export function AppFooter() {
  return null;
}
